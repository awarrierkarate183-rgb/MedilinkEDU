// Optional gold rule helper. Content stays visible. Do not hide blocks until scroll.

(function () {
  document.querySelectorAll(".rule").forEach((el) => {
    el.classList.add("is-on");
  });
})();
